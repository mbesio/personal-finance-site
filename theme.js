import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  typography: {
    articleCard: {
      color: '#3f51b5',
    },
  },
  palette: {
    primary: {
      main: '#fff',
    },
  },
  background: {
    default: '#fff',
  },
})

export default theme
